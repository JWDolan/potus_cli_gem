#!/bin/bash

set -euo pipefail
#set -e => exit if command exits w/ non-zero exit status
#set -u => exit if variable is undefined
#set -o pitfall => return value of pipeline = last command to exit w/ a non-zero status

IFS=$’\n\t' #internal field operator = character list used to split words and lines into words (w/ read command)

bundle install	#sets up Ruby dependencies
