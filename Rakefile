require "bundler/gem_tasks"
require "rspec/core/rake_task" #adds RSpec related modules and classes

RSpec::Core::RakeTask.new(:spec) #creates new RakeTask instance and defines options for the test suite

task :default => :spec   #defines the default rake task
